# 🧠 NeuroChat-IA-v2 - Règles CursorAI
# Configuration personnalisée pour un assistant IA intelligent et sécurisé

## 🎯 CONTEXTE DU PROJET
NeuroChat-IA-v2 est une application de chat IA avancée avec :
- Support multi-providers (OpenAI, Gemini, Mistral) avec fallback automatique
- Chiffrement AES-256-GCM niveau gouvernemental (PBKDF2 600k itérations)
- RAG (Retrieval-Augmented Generation) local avec embeddings MiniLM
- Synthèse vocale et reconnaissance vocale (Web Speech API)
- Interface responsive et accessible avec design moderne
- Gestion des espaces de travail et modes de sécurité
- Mémoire utilisateur avec extraction automatique de faits
- Recherche web (Tavily + DuckDuckGo fallback)
- Modes de sécurité : Normal, Privé (auto-destruction), Enfant (PIN)

## 🏗️ ARCHITECTURE ET ORGANISATION

### Structure des dossiers
```
src/
├── components/          # Composants React réutilisables
│   ├── ui/             # Composants UI de base (shadcn/ui + Radix UI)
│   └── [feature]/      # Composants spécifiques aux fonctionnalités
├── hooks/              # Hooks React personnalisés
├── services/           # Services métier et API
├── lib/                # Utilitaires et helpers
├── types/              # Types TypeScript
└── styles/             # Styles globaux et CSS modules
```

### Conventions de nommage
- **Composants** : PascalCase (`ChatContainer.tsx`, `Header.tsx`)
- **Hooks** : camelCase avec préfixe `use` (`useTheme.ts`, `useWorkspace.ts`)
- **Services** : camelCase (`openaiApi.ts`, `encryption.ts`)
- **Types/Interfaces** : PascalCase (`Message`, `RagContext`)
- **Constantes** : UPPER_SNAKE_CASE (`DEFAULT_TIMEOUT`, `MAX_RETRIES`)

## 🔧 CONVENTIONS TECHNIQUES

### TypeScript
- **Strict mode** : Toujours activé (`strict: true`)
- **Types explicites** : Préférer les types explicites aux `any`
- **Interfaces** : Utiliser pour les objets complexes
- **Generics** : Utiliser quand approprié pour la réutilisabilité
- **Union types** : Pour les valeurs multiples possibles
- **Target** : ES2020 avec moduleResolution "bundler"

```typescript
// ✅ BON
interface Message {
  id: string;
  text: string;
  isUser: boolean;
  timestamp: Date;
  imageUrl?: string;
  memoryFactsCount?: number;
  sources?: Array<{ title: string; url: string }>;
}

// ❌ MAUVAIS
const message: any = { ... };
```

### React
- **Fonctional Components** : Toujours utiliser des composants fonctionnels
- **Hooks** : Respecter les règles des hooks React
- **Props** : Définir des interfaces claires pour les props
- **State** : Utiliser `useState` pour l'état local, `useReducer` pour l'état complexe
- **Effects** : Spécifier les dépendances dans `useEffect`
- **Lazy Loading** : Utiliser `lazy()` et `Suspense` pour les composants lourds
- **Virtualization** : Utiliser `react-virtuoso` pour les longues listes

```typescript
// ✅ BON - Lazy loading des composants
const TTSSettingsModalLazy = lazy(() => import('@/components/TTSSettingsModal').then(m => ({ default: m.TTSSettingsModal })));

// ✅ BON - Props typées
interface ChatContainerProps {
  messages: Message[];
  isLoading: boolean;
  onEditMessage: (id: string, newText: string) => void;
  onDeleteMessage: (id: string) => void;
  onReplyToMessage: (id: string) => void;
  selectMode?: boolean;
  selectedMessageIds?: string[];
  onSelectMessage?: (id: string) => void;
  modePrive?: boolean;
  modeEnfant?: boolean;
}
```

### Tailwind CSS
- **Classes utilitaires** : Utiliser les classes Tailwind natives
- **Responsive** : Toujours penser mobile-first
- **Dark mode** : Supporter `dark:` pour tous les composants
- **Custom classes** : Créer des classes personnalisées pour la réutilisabilité
- **Animations** : Utiliser `tailwindcss-animate` et classes personnalisées
- **Gradients** : Utiliser les gradients pour les modes de sécurité

```typescript
// ✅ BON - Design moderne avec gradients et animations
<div className={cn(
  "flex-1 relative transition-all duration-700 group",
  modePrive 
    ? "bg-gradient-to-br from-red-50/40 via-purple-50/50 to-blue-50/40 dark:from-red-950/30 dark:via-purple-950/40 dark:to-blue-950/30" 
    : modeEnfant
    ? "bg-gradient-to-br from-pink-50/50 via-yellow-50/60 to-orange-50/50 dark:from-pink-950/30 dark:via-yellow-950/40 dark:to-orange-950/30"
    : "bg-gradient-to-br from-slate-50/70 via-white/90 to-blue-50/50 dark:from-slate-900/70 dark:via-slate-900/90 dark:to-slate-800/50",
  "backdrop-blur-2xl"
)}>
```

### UI Components (shadcn/ui + Radix UI)
- **Radix UI** : Utiliser pour l'accessibilité et le comportement
- **shadcn/ui** : Composants de base stylisés avec Tailwind
- **Composants personnalisés** : Étendre les composants de base selon les besoins
- **Accessibilité** : Respecter les standards ARIA et WCAG

## 🎨 STYLE ET QUALITÉ DU CODE

### Formatage
- **Indentation** : 2 espaces
- **Longueur de ligne** : Max 100 caractères
- **Semicolons** : Toujours utiliser
- **Guillemets** : Préférer les guillemets simples pour les chaînes
- **Virgules trailing** : Utiliser pour les objets et arrays multilignes

### Commentaires
- **JSDoc** : Pour les fonctions et composants publics
- **Commentaires explicatifs** : Pour la logique complexe
- **TODO/FIXME** : Marquer les améliorations futures
- **Sécurité** : Documenter les aspects de sécurité critiques

```typescript
/**
 * 🔐 Service de Chiffrement AES-256 - Niveau Gouvernemental
 * 
 * Implémentation de sécurité militaire pour NeuroChat
 * - Chiffrement AES-256-GCM (Galois/Counter Mode) 
 * - Dérivation de clé PBKDF2 avec 600,000 itérations
 * - Salt cryptographiquement sécurisé de 256 bits
 * - Vecteur d'initialisation (IV) unique par opération
 * - Authentification intégrée (AEAD)
 * - Gestion sécurisée des clés en mémoire
 */
```

### Gestion des erreurs
- **Try-catch** : Toujours gérer les erreurs async
- **Error boundaries** : Implémenter pour les composants React
- **Logging** : Logger les erreurs de manière appropriée
- **Fallbacks** : Fournir des alternatives en cas d'échec
- **Toast notifications** : Utiliser `sonner` pour les retours utilisateur

```typescript
// ✅ BON - Gestion d'erreurs avec fallback
try {
  const result = await apiCall();
  return result;
} catch (error) {
  console.error('Erreur API:', error);
  toast.error('Impossible de récupérer les données');
  // Fallback ou re-throw selon le contexte
  throw new Error('Impossible de récupérer les données');
}
```

## 🚀 RÈGLES SPÉCIFIQUES AU PROJET

### Sécurité et Chiffrement
- **AES-256-GCM** : Toujours utiliser pour le chiffrement des données sensibles
- **PBKDF2** : 600,000 itérations pour la dérivation de clé
- **Clés** : Ne jamais exposer les clés dans le code
- **Validation** : Valider toutes les entrées utilisateur
- **Sanitisation** : Nettoyer les données avant affichage
- **Modes de sécurité** : Normal, Privé (auto-destruction), Enfant (PIN)

### Performance
- **Lazy loading** : Pour les composants lourds (modales, paramètres)
- **Memoization** : Utiliser `useMemo` et `useCallback` judicieusement
- **Virtualization** : Pour les longues listes (react-virtuoso)
- **Bundle splitting** : Diviser le code en chunks avec Vite
- **Code splitting** : Importer dynamiquement les composants non critiques

### Accessibilité
- **ARIA labels** : Toujours fournir des labels accessibles
- **Navigation clavier** : Supporter la navigation au clavier
- **Contraste** : Respecter les ratios de contraste WCAG
- **Screen readers** : Tester avec des lecteurs d'écran
- **Radix UI** : Utiliser les composants accessibles par défaut

### Fonctionnalités Spéciales
- **RAG local** : Embeddings MiniLM avec similarité cosinus
- **Mémoire utilisateur** : Extraction automatique de faits + fallback LLM
- **Recherche web** : Tavily + DuckDuckGo fallback
- **Synthèse vocale** : Configuration avancée (vitesse, tonalité, volume)
- **Reconnaissance vocale** : Web Speech API avec support fr-FR
- **Modes de sécurité** : Gestion des espaces de travail chiffrés

## 🤖 RÈGLES CURSORAI

### Génération de code
- **Composants modulaires** : Créer des composants réutilisables et testables
- **Props typées** : Toujours définir des interfaces claires
- **Hooks personnalisés** : Extraire la logique dans des hooks réutilisables
- **Gestion d'état** : Utiliser des patterns React modernes
- **Lazy loading** : Implémenter pour les composants lourds
- **Virtualization** : Utiliser react-virtuoso pour les listes longues

### Refactoring
- **Extraction** : Diviser les gros composants en plus petits
- **Réutilisabilité** : Créer des composants génériques
- **Performance** : Optimiser les re-renders et la mémoire
- **Tests** : Rendre le code testable
- **Code splitting** : Séparer les fonctionnalités non critiques

### Documentation
- **README** : Maintenir à jour avec les nouvelles fonctionnalités
- **JSDoc** : Documenter les APIs publiques
- **Exemples** : Fournir des exemples d'utilisation
- **Changelog** : Documenter les changements
- **Sécurité** : Documenter les aspects de sécurité

## 📝 CONVENTIONS GIT

### Messages de commit
```
type(scope): description courte

Description détaillée si nécessaire

- Changement 1
- Changement 2

Fixes #123
```

**Types** : `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`
**Scope** : `ui`, `api`, `security`, `performance`, `mobile`, `voice`, `rag`, `memory`

### Branches
- **main** : Code de production
- **develop** : Développement principal
- **feature/*** : Nouvelles fonctionnalités
- **fix/*** : Corrections de bugs
- **hotfix/*** : Corrections urgentes

## 🔍 VÉRIFICATIONS QUALITÉ

### Avant commit
- [ ] Code compile sans erreurs TypeScript
- [ ] Tests passent (si implémentés)
- [ ] Linting OK (ESLint + Prettier)
- [ ] Types TypeScript valides
- [ ] Responsive design vérifié
- [ ] Accessibilité testée
- [ ] Sécurité vérifiée (chiffrement)

### Code review
- [ ] Logique claire et compréhensible
- [ ] Performance acceptable
- [ ] Sécurité respectée
- [ ] Tests couvrent les cas d'usage
- [ ] Documentation à jour
- [ ] Lazy loading implémenté si nécessaire

## 🎯 PROMPTS RECOMMANDÉS

### Pour la création de composants
```
Crée un composant React TypeScript pour [fonctionnalité] avec :
- Interface claire et accessible (Radix UI + shadcn/ui)
- Support du mode sombre et des gradients de sécurité
- Responsive design mobile-first avec Tailwind
- Gestion d'erreurs appropriée avec toast notifications
- Lazy loading si le composant est lourd
- Tests unitaires de base
```

### Pour le refactoring
```
Refactorise ce composant en suivant les bonnes pratiques :
- Divise en composants plus petits et réutilisables
- Extrais la logique dans des hooks personnalisés
- Améliore la performance avec memoization
- Ajoute la gestion d'erreurs avec try-catch
- Implémente le lazy loading si nécessaire
- Rends le code plus testable
```

### Pour l'optimisation
```
Optimise ce code pour :
- Réduire les re-renders avec useMemo/useCallback
- Améliorer la performance avec react-virtuoso
- Réduire la taille du bundle avec code splitting
- Améliorer l'accessibilité avec ARIA labels
- Respecter les conventions du projet
- Implémenter le lazy loading des composants lourds
```

### Pour la sécurité
```
Implémente la sécurité pour [fonctionnalité] :
- Chiffrement AES-256-GCM avec PBKDF2 600k itérations
- Validation stricte des inputs utilisateur
- Gestion sécurisée des clés en mémoire
- Modes de sécurité (Normal, Privé, Enfant)
- Protection contre les attaques XSS
- Logging sécurisé des événements critiques
```

## 📚 RESSOURCES UTILES

- **React Docs** : https://react.dev/
- **TypeScript Handbook** : https://www.typescriptlang.org/docs/
- **Tailwind CSS** : https://tailwindcss.com/docs
- **shadcn/ui** : https://ui.shadcn.com/
- **Radix UI** : https://www.radix-ui.com/
- **ESLint Rules** : https://eslint.org/docs/rules/
- **Accessibility Guidelines** : https://www.w3.org/WAI/WCAG21/quickref/
- **Crypto Web API** : https://developer.mozilla.org/en-US/docs/Web/API/Web_Crypto_API
- **Web Speech API** : https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API

---

*Ce fichier .cursorrules est vivant et doit être mis à jour selon l'évolution du projet et les nouvelles bonnes pratiques.*
